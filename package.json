{
  "name": "consentbit-webflow-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "kill-port 1337",
    "dev": "vite build && concurrently -k -n vite,webflow -c blue,magenta \"vite build --watch\" \"node scripts/serve.mjs\"",
    "predev:test": "kill-port 1337 && node scripts/testOrigins.mjs add",
    "dev:test": "vite build --mode test && concurrently -k -n vite,webflow -c blue,magenta \"vite build --watch --mode test\" \"node scripts/serve.mjs\"",
    "dev:vite": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "wf:bundle": "node scripts/testOrigins.mjs remove && vite build && node scripts/bundle.mjs",
    "wf:bundle:test": "node scripts/bundleTest.mjs"
  },
  "dependencies": {
    "@jaames/iro": "^5.5.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^10.0.3",
    "kill-port": "^2.0.1",
    "vite": "^5.4.11"
  }
}
