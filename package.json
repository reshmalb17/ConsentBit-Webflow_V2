{
  "name": "consentbit-webflow-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "kill-port 1337",
    "dev": "vite build && concurrently -k -n vite,webflow -c blue,magenta \"vite build --watch\" \"webflow extension serve\"",
    "dev:vite": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "wf:bundle": "vite build && webflow extension bundle"
  },
  "dependencies": {
    "@jaames/iro": "^5.5.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^10.0.3",
    "kill-port": "^2.0.1",
    "vite": "^5.4.11"
  }
}
