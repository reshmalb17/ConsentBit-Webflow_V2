# `npm run dev` preview:
#   VITE_GALLERY=0  → the extension Landing screen (framed 800×560) — what the
#                     Webflow panel shows. (default)
#   VITE_GALLERY=1  → the full screen gallery (browse every screen).
VITE_GALLERY=0
